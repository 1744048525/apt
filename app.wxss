/* 设置全局css */
/* 小程序不支持*通配符选择器 */
/* 引入字体图标库 */
@import "./styles/iconfont.wxss";
/* 初始化工作 */
view,
text,
image,
navigator,
swiper,
swiper-item {
  padding: 0rpx;
  margin: 0rpx;
  box-sizing: border-box;
}
/* 让图片默认占满整个容器的宽度 */
image {
  width: 100%;
  height: 100%;
}
page {
  /* 相当于定义了一个变量,存储了一个颜色值，如果其他地方需要用到该颜色,可直接引用 */
  /* 又何用处：如果网页中有大量地方引用了这个颜色变量,如果需要将这些地方的颜色值都修改的话,只需要改变这里的颜色值就可以了,相当于网页换肤的功能 */
  --themeColor:#f93553;
}
